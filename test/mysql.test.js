var mysql = require('../mysql.js');

describe('MySqlClient', function () {
    describe('#select(query, parameters, callback)', function() {
        it('should do something', function () {
            // perform test
        });
    });
});